import MySQLdb
import matplotlib.pyplot as plt
from matplotlib.finance import candlestick_ohlc

conn = MySQLdb.connect(host = "localhost", user = "root", passwd = "dealer1073.", db = "aetos", port = 3306, charset = "utf8")
cur = conn.cursor(cursorclass = MySQLdb.cursors.DictCursor)

#tablename = str(raw_input('Please input the table you want to operate:  '))
#cur.execute("select * from %s" % tablename)


symbol_name = raw_input('Please Enter the Symbol Name:\t')

cur.execute("select * from orders where Symbol = '%s'" % symbol_name)

lines = cur.fetchall()

for line in lines:
    print line


'''
login,op,vol,bs = ([] for i in range(4))

for line in lines:
    login.append(line['Login'])
    bs.append(line['Type'])
    op.append(line['OpenPrice'])
    vol.append(line['Volume'])
   
'''





cur.close()
conn.close()
